http.bind.port: 8081
http.bind.ip: 127.0.0.1

tentacles:
   - host: localhost
     # default port 8080 if not specified
     # port: 8080
     # currently supported protocols: http, https
     # protocol: http
  # - host: other host
      # default port 8080 if not specified
      # port: 8080
      # currently supported protocols: http, https
      # protocol: http
